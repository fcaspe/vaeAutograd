# Variaional Autoencoder using automatic differentiation.

Execute <code> vae.py </code> to train the Fully-Connected neural network and obtain the results.
Requires the <code> autograd </code> Python package to work.